(function() {
  var Graph;

  Graph = (function() {

    Graph.name = 'Graph';

    function Graph(width, height) {
      this.width = width;
      this.height = height;
    }

    return Graph;

  })();

}).call(this);
